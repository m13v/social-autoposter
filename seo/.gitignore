# GSC pipeline runtime artifacts — never commit
credentials/
logs/
state/fazm/
state/pieline/
.locks/
.refresh_timestamps/
__pycache__/
*.pyc

# Generated SEO content drafts (1,400+ files); the initial Apr-12 batch is
# already tracked and stays tracked — this rule only affects new drafts.
concepts/
escalations.log
